/**
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExperimentVerdict model module.
 * @module model/ExperimentVerdict
 * @version 25.17.0
 */
class ExperimentVerdict {
    /**
     * Constructs a new <code>ExperimentVerdict</code>.
     * @alias module:model/ExperimentVerdict
     * @param winnerVariantName {String} The name of the winning variant. If no variant shows a statistically significant advantage on key business metrics, return 'Inconclusive'.
     * @param verdictSummary {String} A one-sentence summary of the outcome, including the key metric and confidence level that led to the decision.
     * @param keyFindings {Array.<String>} A bullet point stating the most important finding, including the metric, the percentage change, and the confidence.
     * @param aiConfidenceLevel {String} Your confidence in this overall verdict, from 0 to 100.
     * @param recommendation {String} A short, actionable recommendation based on the findings. If inconclusive, suggest running the test longer. If there is a clear winner, recommend promoting it.
     */
    constructor(winnerVariantName, verdictSummary, keyFindings, aiConfidenceLevel, recommendation) { 
        
        ExperimentVerdict.initialize(this, winnerVariantName, verdictSummary, keyFindings, aiConfidenceLevel, recommendation);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, winnerVariantName, verdictSummary, keyFindings, aiConfidenceLevel, recommendation) { 
        obj['winnerVariantName'] = winnerVariantName;
        obj['verdictSummary'] = verdictSummary;
        obj['keyFindings'] = keyFindings;
        obj['aiConfidenceLevel'] = aiConfidenceLevel;
        obj['recommendation'] = recommendation;
    }

    /**
     * Constructs a <code>ExperimentVerdict</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExperimentVerdict} obj Optional instance to populate.
     * @return {module:model/ExperimentVerdict} The populated <code>ExperimentVerdict</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExperimentVerdict();

            if (data.hasOwnProperty('winnerVariantName')) {
                obj['winnerVariantName'] = ApiClient.convertToType(data['winnerVariantName'], 'String');
            }
            if (data.hasOwnProperty('verdictSummary')) {
                obj['verdictSummary'] = ApiClient.convertToType(data['verdictSummary'], 'String');
            }
            if (data.hasOwnProperty('keyFindings')) {
                obj['keyFindings'] = ApiClient.convertToType(data['keyFindings'], ['String']);
            }
            if (data.hasOwnProperty('aiConfidenceLevel')) {
                obj['aiConfidenceLevel'] = ApiClient.convertToType(data['aiConfidenceLevel'], 'String');
            }
            if (data.hasOwnProperty('recommendation')) {
                obj['recommendation'] = ApiClient.convertToType(data['recommendation'], 'String');
            }
        }
        return obj;
    }


}

/**
 * The name of the winning variant. If no variant shows a statistically significant advantage on key business metrics, return 'Inconclusive'.
 * @member {String} winnerVariantName
 */
ExperimentVerdict.prototype['winnerVariantName'] = undefined;

/**
 * A one-sentence summary of the outcome, including the key metric and confidence level that led to the decision.
 * @member {String} verdictSummary
 */
ExperimentVerdict.prototype['verdictSummary'] = undefined;

/**
 * A bullet point stating the most important finding, including the metric, the percentage change, and the confidence.
 * @member {Array.<String>} keyFindings
 */
ExperimentVerdict.prototype['keyFindings'] = undefined;

/**
 * Your confidence in this overall verdict, from 0 to 100.
 * @member {String} aiConfidenceLevel
 */
ExperimentVerdict.prototype['aiConfidenceLevel'] = undefined;

/**
 * A short, actionable recommendation based on the findings. If inconclusive, suggest running the test longer. If there is a clear winner, recommend promoting it.
 * @member {String} recommendation
 */
ExperimentVerdict.prototype['recommendation'] = undefined;






export default ExperimentVerdict;

