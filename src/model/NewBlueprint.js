/**
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CartItemFilterTemplate from './CartItemFilterTemplate';
import CatalogRule from './CatalogRule';

/**
 * The NewBlueprint model module.
 * @module model/NewBlueprint
 * @version 25.17.0
 */
class NewBlueprint {
    /**
     * Constructs a new <code>NewBlueprint</code>.
     * @alias module:model/NewBlueprint
     * @param title {String} The display name for the blueprint.
     */
    constructor(title) { 
        
        NewBlueprint.initialize(this, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, title) { 
        obj['title'] = title;
    }

    /**
     * Constructs a <code>NewBlueprint</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NewBlueprint} obj Optional instance to populate.
     * @return {module:model/NewBlueprint} The populated <code>NewBlueprint</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NewBlueprint();

            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], [CatalogRule]);
            }
            if (data.hasOwnProperty('cartItemFilters')) {
                obj['cartItemFilters'] = ApiClient.convertToType(data['cartItemFilters'], [CartItemFilterTemplate]);
            }
        }
        return obj;
    }


}

/**
 * The display name for the blueprint.
 * @member {String} title
 */
NewBlueprint.prototype['title'] = undefined;

/**
 * A longer, more detailed description of the blueprint.
 * @member {String} description
 */
NewBlueprint.prototype['description'] = undefined;

/**
 * Category used to group blueprints.
 * @member {module:model/NewBlueprint.CategoryEnum} category
 * @default 'custom'
 */
NewBlueprint.prototype['category'] = 'custom';

/**
 * Array of rules to store in this blueprint. Rules should only contain title (no description, as description is at the blueprint level). At least one rule or cart item filter is required.
 * @member {Array.<module:model/CatalogRule>} rules
 */
NewBlueprint.prototype['rules'] = undefined;

/**
 * Array of cart item filters to store in this blueprint. If not provided, will be extracted from the rules. Cart item filters should only contain name (no description, as description is at the blueprint level).
 * @member {Array.<module:model/CartItemFilterTemplate>} cartItemFilters
 */
NewBlueprint.prototype['cartItemFilters'] = undefined;





/**
 * Allowed values for the <code>category</code> property.
 * @enum {String}
 * @readonly
 */
NewBlueprint['CategoryEnum'] = {

    /**
     * value: "promotions"
     * @const
     */
    "promotions": "promotions",

    /**
     * value: "pricing"
     * @const
     */
    "pricing": "pricing",

    /**
     * value: "loyalty"
     * @const
     */
    "loyalty": "loyalty",

    /**
     * value: "custom"
     * @const
     */
    "custom": "custom"
};



export default NewBlueprint;

