/**
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExperimentSegmentInsightVariant from './ExperimentSegmentInsightVariant';

/**
 * The ExperimentSegmentInsight model module.
 * @module model/ExperimentSegmentInsight
 * @version 25.17.0
 */
class ExperimentSegmentInsight {
    /**
     * Constructs a new <code>ExperimentSegmentInsight</code>.
     * @alias module:model/ExperimentSegmentInsight
     * @param dimension {module:model/ExperimentSegmentInsight.DimensionEnum} The segmentation dimension used to group customers or purchases for analysis.
     * @param bucket {module:model/ExperimentSegmentInsight.BucketEnum} The specific group within the segmentation dimension.
     * @param confidence {Number} The raw (unadjusted) confidence score expressed as a percentage. Only segments with a confidence score greater than or equal to 95% are returned. 
     * @param winnerVariantId {Number} The ID of the variant that performed better in this segment.
     * @param variants {Array.<module:model/ExperimentSegmentInsightVariant>} Per-variant metric values for this segment.
     */
    constructor(dimension, bucket, confidence, winnerVariantId, variants) { 
        
        ExperimentSegmentInsight.initialize(this, dimension, bucket, confidence, winnerVariantId, variants);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dimension, bucket, confidence, winnerVariantId, variants) { 
        obj['dimension'] = dimension;
        obj['bucket'] = bucket;
        obj['confidence'] = confidence;
        obj['winnerVariantId'] = winnerVariantId;
        obj['variants'] = variants;
    }

    /**
     * Constructs a <code>ExperimentSegmentInsight</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExperimentSegmentInsight} obj Optional instance to populate.
     * @return {module:model/ExperimentSegmentInsight} The populated <code>ExperimentSegmentInsight</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExperimentSegmentInsight();

            if (data.hasOwnProperty('dimension')) {
                obj['dimension'] = ApiClient.convertToType(data['dimension'], 'String');
            }
            if (data.hasOwnProperty('bucket')) {
                obj['bucket'] = ApiClient.convertToType(data['bucket'], 'String');
            }
            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('winnerVariantId')) {
                obj['winnerVariantId'] = ApiClient.convertToType(data['winnerVariantId'], 'Number');
            }
            if (data.hasOwnProperty('variants')) {
                obj['variants'] = ApiClient.convertToType(data['variants'], [ExperimentSegmentInsightVariant]);
            }
        }
        return obj;
    }


}

/**
 * The segmentation dimension used to group customers or purchases for analysis.
 * @member {module:model/ExperimentSegmentInsight.DimensionEnum} dimension
 */
ExperimentSegmentInsight.prototype['dimension'] = undefined;

/**
 * The specific group within the segmentation dimension.
 * @member {module:model/ExperimentSegmentInsight.BucketEnum} bucket
 */
ExperimentSegmentInsight.prototype['bucket'] = undefined;

/**
 * The raw (unadjusted) confidence score expressed as a percentage. Only segments with a confidence score greater than or equal to 95% are returned. 
 * @member {Number} confidence
 */
ExperimentSegmentInsight.prototype['confidence'] = undefined;

/**
 * The ID of the variant that performed better in this segment.
 * @member {Number} winnerVariantId
 */
ExperimentSegmentInsight.prototype['winnerVariantId'] = undefined;

/**
 * Per-variant metric values for this segment.
 * @member {Array.<module:model/ExperimentSegmentInsightVariant>} variants
 */
ExperimentSegmentInsight.prototype['variants'] = undefined;





/**
 * Allowed values for the <code>dimension</code> property.
 * @enum {String}
 * @readonly
 */
ExperimentSegmentInsight['DimensionEnum'] = {

    /**
     * value: "cart_value"
     * @const
     */
    "cart_value": "cart_value",

    /**
     * value: "item_count"
     * @const
     */
    "item_count": "item_count",

    /**
     * value: "customer_type"
     * @const
     */
    "customer_type": "customer_type"
};


/**
 * Allowed values for the <code>bucket</code> property.
 * @enum {String}
 * @readonly
 */
ExperimentSegmentInsight['BucketEnum'] = {

    /**
     * value: "low"
     * @const
     */
    "low": "low",

    /**
     * value: "medium"
     * @const
     */
    "medium": "medium",

    /**
     * value: "high"
     * @const
     */
    "high": "high",

    /**
     * value: "new"
     * @const
     */
    "new": "new",

    /**
     * value: "returning"
     * @const
     */
    "returning": "returning",

    /**
     * value: "loyal"
     * @const
     */
    "loyal": "loyal"
};



export default ExperimentSegmentInsight;

