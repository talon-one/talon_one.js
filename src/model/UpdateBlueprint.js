/**
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CartItemFilterTemplate from './CartItemFilterTemplate';
import CatalogRule from './CatalogRule';

/**
 * The UpdateBlueprint model module.
 * @module model/UpdateBlueprint
 * @version 25.17.0
 */
class UpdateBlueprint {
    /**
     * Constructs a new <code>UpdateBlueprint</code>.
     * @alias module:model/UpdateBlueprint
     */
    constructor() { 
        
        UpdateBlueprint.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UpdateBlueprint</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateBlueprint} obj Optional instance to populate.
     * @return {module:model/UpdateBlueprint} The populated <code>UpdateBlueprint</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateBlueprint();

            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], [CatalogRule]);
            }
            if (data.hasOwnProperty('cartItemFilters')) {
                obj['cartItemFilters'] = ApiClient.convertToType(data['cartItemFilters'], [CartItemFilterTemplate]);
            }
        }
        return obj;
    }


}

/**
 * The display name for the blueprint.
 * @member {String} title
 */
UpdateBlueprint.prototype['title'] = undefined;

/**
 * A longer, more detailed description of the blueprint.
 * @member {String} description
 */
UpdateBlueprint.prototype['description'] = undefined;

/**
 * Category used to group blueprints.
 * @member {module:model/UpdateBlueprint.CategoryEnum} category
 */
UpdateBlueprint.prototype['category'] = undefined;

/**
 * Replaces the stored rules. Rules should only contain title (no description, as description is at the blueprint level).
 * @member {Array.<module:model/CatalogRule>} rules
 */
UpdateBlueprint.prototype['rules'] = undefined;

/**
 * Replaces the stored cart item filters. Cart item filters should only contain name (no description, as description is at the blueprint level).
 * @member {Array.<module:model/CartItemFilterTemplate>} cartItemFilters
 */
UpdateBlueprint.prototype['cartItemFilters'] = undefined;





/**
 * Allowed values for the <code>category</code> property.
 * @enum {String}
 * @readonly
 */
UpdateBlueprint['CategoryEnum'] = {

    /**
     * value: "promotions"
     * @const
     */
    "promotions": "promotions",

    /**
     * value: "pricing"
     * @const
     */
    "pricing": "pricing",

    /**
     * value: "loyalty"
     * @const
     */
    "loyalty": "loyalty",

    /**
     * value: "custom"
     * @const
     */
    "custom": "custom"
};



export default UpdateBlueprint;

